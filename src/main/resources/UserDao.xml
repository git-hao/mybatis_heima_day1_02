<?xml version="1.0" encoding="UTF-8"?>


<!--mybatis映射文件位置必须和dao接口的包结构相同，namespace是接口全限定类名-->
<mapper namespace="com.hao.dao.UserDao">
    <!--配置方法，id为dao方法名-->
    <select id="findAll" resultType="com.hao.domain.User">
        select * from  user
    </select>
</mapper>
